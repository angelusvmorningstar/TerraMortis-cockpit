# Local environment — never commit connection strings.
.env

# Generated drafting packs are disposable output. Keep the directory (.gitkeep), ignore its contents.
out/*
!out/.gitkeep

# Dependencies.
node_modules/

# Sandbox seed dump output — one-time setup, may carry player PII; never commit.
_seed/
